This program is made to the specifications on the assignment 1 outline.
It reads a standard text file with movie information and puts it into an arrayList
of objects "movie". It present the user with an terminal menu that allows it to 
pick several options such as add a move, search by year, and provide a random list. 
When the user if finished the current arrayList is saved back into the text file. 